name: CI

on: [push] 

jobs:
    run-shellscript-file:
      runs-on: ubuntu-latest                
      steps: 
          - name: Checkout code
          - uses: actions/checkout@v2            
          - name: Run a one-line script               
             run: ./name.sh
